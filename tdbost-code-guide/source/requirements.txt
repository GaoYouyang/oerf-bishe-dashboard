# 【文件作用】列出 Python 运行环境需要安装的软件包及指定版本。
# 安装命令：pip install -r requirements.txt
# 注释行以 # 开头，pip 会忽略它们。

# 快速 Kolmogorov-Arnold Network 实现，供可选网络模块使用。
fastkan==0.0.1
# 读取和写入图像/视频帧。
imageio==2.37.2
# 基于 PyTorch 的计算机视觉算子。
kornia==0.6.10
# 感知图像相似度指标 LPIPS。
lpips==0.1.4
# 绘图和保存三维场切片。
matplotlib==3.10.8
# CPU 数值数组与线性代数。
numpy==2.4.4
# 结构化配置对象。
omegaconf==2.3.0
# 包含扩展模块的 OpenCV 发行版。
opencv_contrib_python==4.13.0.90
# 标准 OpenCV Python 发行版。
opencv_python==4.9.0.80
# 无图形界面的 OpenCV 发行版，适合服务器。
opencv_python_headless==4.13.0.92
# Python 图像处理库。
Pillow==12.2.0
# 实验设计采样库。
# 【注意】原仓库同时固定了 pyDOE 的两个不同版本，pip 通常会报告版本冲突。
pyDOE==0.3.8
# 第二条 pyDOE 版本约束按师兄原代码保留，没有擅自修改。
pyDOE==0.9.8
# PyTorch 版多尺度结构相似性指标。
pytorch_msssim==0.2.1
# ZeroMQ 的 Python 接口。
pyzmq==25.1.2
# 插值、MATLAB 文件、滤波等科学计算工具。
scipy==1.17.1
# 原仓库写的是 skimage==0.0；常见正式包名通常是 scikit-image。
# 这里按原文件保留，避免注释版偷偷改变环境定义。
skimage==0.0
# 核心深度学习与张量计算框架。
torch==2.9.1
# PyTorch 的计算机视觉工具。
torchvision==0.24.1
# 把 PyTorch 计算图画出来。
torchviz==0.0.2
# 终端进度条。
tqdm==4.65.0
