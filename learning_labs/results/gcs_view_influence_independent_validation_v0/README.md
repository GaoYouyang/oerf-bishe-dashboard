# Independent view-influence result validation

Status: `VALID_NO_GO_DECISIVE_INCREMENTAL_GATE_FAILURE`. ROC-AUC is recomputed by explicit positive-negative pair concordance rather than the runner's rank implementation. The decisive incremental gate is independently NO-GO.
