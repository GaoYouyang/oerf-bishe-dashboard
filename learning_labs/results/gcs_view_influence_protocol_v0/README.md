# View-influence protocol validation

`validation.json` verifies the post-open exact leave-one-view protocol, its
source hashes, complete 12-group denominator, 28-fit pilot cost, 912-fit full
panel cost, truth firewall, group split, and sealed Stage B.

The pilot runner is implemented but not executed in this directory. This
artifact authorizes only the 28-fit engineering pilot. It does not authorize a
mechanism signal, full panel, fresh audit, algorithm, real BOST claim, or paper
result.
