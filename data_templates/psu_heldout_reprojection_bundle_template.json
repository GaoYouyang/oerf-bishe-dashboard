{
  "schema_version": "psu-heldout-reprojection-bundle-1.0",
  "registration_status": "FROZEN_BEFORE_SINGLE_FINAL_AUDIT",
  "protocol_sha256": "replace_with_frozen_protocol_sha256",
  "split_id": "audit_rotation_same_cameras",
  "candidate": {
    "id": "replace_with_candidate_id",
    "checkpoint_sha256": "replace_with_lowercase_sha256",
    "config_sha256": "replace_with_lowercase_sha256"
  },
  "baseline": {
    "id": "B0_FIXED_DOMAIN_REFERENCE",
    "checkpoint_sha256": "replace_with_lowercase_sha256",
    "config_sha256": "replace_with_lowercase_sha256"
  },
  "development_repeatability_floor_px": null,
  "calibration_perturbation_gate_passed": null,
  "views": [
    {
      "rotation_degrees": 10,
      "camera_id": 2,
      "npz": "views/rotation_010_camera_02.npz",
      "sha256": "replace_with_lowercase_sha256"
    }
  ],
  "npz_contract": {
    "measured_uv_px": "float array, shape (N, 2)",
    "candidate_uv_px": "float array, shape (N, 2)",
    "baseline_uv_px": "float array, shape (N, 2)",
    "active_mask": "boolean array, shape (N,)",
    "ambient_mask": "boolean array, shape (N,)",
    "front_band_mask": "boolean active-subset array, shape (N,), frozen top-gradient diagnostic"
  },
  "required_view_grid": {
    "camera_ids": [
      2,
      3,
      4
    ],
    "rotation_degrees": [
      10,
      20,
      30,
      60,
      70,
      80
    ],
    "view_count": 18
  }
}
