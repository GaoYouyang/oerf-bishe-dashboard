# v5f dual-regularization post-open development

The optical radius comes from the saved v5e df-corrected discrepancy decision. The final field is independently refit with GCV or UPRE. Outer cameras may route; truth and the audit camera are opened only for this post-open verdict. The same v5c fields and rigs are reused, so no result in this folder is confirmatory.
