# v5f outer-to-audit post-open NO-GO

The GCV and UPRE refits are identical on these blocks. For the 24 rows where calibration changes radius, the worst-outer versus audit gain correlation is negative and sign agreement is below one half. The only two outer-nonworse routes both harm the audit camera. Threshold retuning is therefore forbidden; it would fit the already opened audit data rather than repair view-specific transfer.
