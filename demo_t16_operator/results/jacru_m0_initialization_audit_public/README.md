# JACRU-M0 initialization audit

This audit scores the declared interface before any observation is used. It checks whether the apparent interface gain is inherited from a geometry-aligned initialization rather than learned from BOST data.
