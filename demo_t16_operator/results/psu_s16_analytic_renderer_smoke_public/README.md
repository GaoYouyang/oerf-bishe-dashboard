# PSU-S16 independent-renderer smoke

This is an E1 synthetic interface check on real PSU support geometry. Analytic gradients generate the observations; the inverse uses the voxel-gradient operator. It is not experimental reconstruction, CFD validation, fresh confirmation, or evidence that a learned operator is better than a baseline.
