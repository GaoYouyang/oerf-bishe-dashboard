{
  "name": "t16_v3e_cross_architecture_compute_accounting_v1",
  "dataset_config": "direct_operator_dataset.json",
  "own_algorithm_config": "own_algorithm_benchmark.json",
  "source_dataset": "t16_direct_operator_fields_v1_dataset.npz",
  "output_dir": "v3e_compute_accounting",
  "total_budget": 6,
  "fixed_query_index": 4,
  "audit_query_index": 3,
  "ridge_relative_grid": [0.000001, 0.00001, 0.0001, 0.001, 0.01, 0.1],
  "methods": [
    "ridge_unet_aug",
    "ridge_fno_aug",
    "ridge_deeponet",
    "ray_set_operator",
    "zero_init_ray_set_adapter"
  ],
  "device": "auto",
  "seed": 20261021,
  "worker_repeats": 3,
  "inference_batch_size": 1,
  "training_batch_size": 12,
  "inference_warmup_steps": 8,
  "inference_measure_steps": 40,
  "training_warmup_steps": 3,
  "training_measure_steps": 20,
  "memory_poll_interval_ms": 1.0,
  "fno_time_to_target_validation_rel_l2": [0.12, 0.10, 0.09],
  "fixed_epoch_checkpoints": [60, 120, 180, 240],
  "compute_contract": {
    "estimate_name": "partial analytical FLOPs-v1 with a declared FFT convention",
    "mac_definition": "one multiplication accumulated into one output",
    "dense_flop_conversion": "two real FLOPs per dense MAC",
    "complex_mac_flop_conversion": "six real FLOPs per complex multiply-accumulate",
    "real_fft_estimate": "2.5 * N * log2(N) real FLOPs per channel and transform",
    "excluded_from_estimate": [
      "normalization",
      "activation",
      "pooling",
      "softmax",
      "indexing",
      "most elementwise operations",
      "optimizer update arithmetic"
    ],
    "timing_scope": "steady-state synchronized forward or full loss-backward-AdamW step",
    "memory_scope": "fresh worker process; sampled MPS allocated/driver bytes plus process peak RSS",
    "claim_scope": "development cost audit only; not an exact hardware FLOP count or superiority result"
  }
}
